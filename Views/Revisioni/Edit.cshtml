@model RevisioneViewModel

<h2>@ViewData["Title"]</h2>

<form asp-action="Edit" method="post">
    <input type="hidden" asp-for="Nag_Affidato" />
    <div class="form-group">
        <label asp-for="NoteIstruttore"></label>
        <textarea asp-for="NoteIstruttore" class="form-control"></textarea>
    </div>
    <button type="submit" class="btn btn-primary">Salva</button>
</form>